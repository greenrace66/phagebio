// Placeholder for type definitions
export interface ContactFormData {
  name: string;
  email: string;
  message: string;
}

// Add any other types that might be expected by other files if necessary
export interface UserProfile {
  id: string;
  credits: number;
  // other profile fields
}
